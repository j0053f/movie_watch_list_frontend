export default function ProgressSquare({ squaresNo, squaresSize }) {
  return <Square />;
}

function Square({ squareSize }) {
  // how many rows it must have?
  //how set the width and height of it?
  // how change the color of it by java script
  // how add a number to it
  return (
    <div>
      <div>1</div>
      <div>2</div>
      <div>3</div>
      <div>4</div>
      <div>5</div>
    </div>
  );
}
